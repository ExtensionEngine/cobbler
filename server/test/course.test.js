'use strict';

const controller = require('../course/course.controller');

describe('Course Service', () => {
  describe('Create new course', () => {
    it('Should be cool', () => {
      expect(true).toBe(true);
    });
  });
});
